mixin filter-status(m)
    each item in m
        //- ở phía dưới là ta tự định nghĩa ra 1 thuộc tính tên là button-status
        button(class=`btn btn-sm ml-1 btn-outline-success ${item.class}` button-status=item.status) #{item.name} 
