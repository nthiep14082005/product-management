extends ../../layouts/default.pug

block bien
    if(product.title) 
        h1(class="mb-4") #{product.title}
    
    if(product.price) 
        div(class="mb-4") Giá: <b>#{product.price}</b>
    
    if(product.discountPercentage) 
        div(class="mb-4") Giảm giá: <b>#{product.discountPercentage}</b>
    
    if(product.stock) 
        div(class="mb-4") Giá: <b>#{product.stock}</b>

    if(product.thumbnail) 
        div(class="mb-4") 
            img(
                src=product.thumbnail
                style="width: 100px"
            )
    
    if(product.status)
        div(class="mb-4")
            span Trạng thái: 
            if(product.status === "active") 
                span(class="badge badge-success") Hoạt động
            else 
                span(class="badge badge-danger") Dừng hoạt động
    
    if(product.position)
        div(class="mb-4") Vị trí: <b>#{product.position}</b>
    
    //- if(product.description)
    //-     div(class="mb-4")
    //-         span Giới thiệu sản phẩm: 
    //-             <b>#{product.description}</b>
    //- ------------------> tại sao không sử dụng # mà lại sử dụng ! bởi vì -> trong pug thì #{} dùng để hiển thị string nên nó sẽ hiển thị dạng string, còn ! thì sẽ chuyển đổi string thành cú pháp HTML nếu có, !!!!!!!!!!! LƯU Ý: Khi lưu vào database thì bắt buộc 100% phải dùng #{} để lưu string.
    if(product.description)
        div(class="mb-4")
            span Giới thiệu sản phẩm: 
                <b>!{product.description}</b>


    a(
        href=`${prefixAdmin}/products/edit-item/${product.id}`
        class="btn btn-warning btn-sm"
    ) Sửa
    
    script(src="/admin/js/product.js")
